<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shikou.aicode.mapper.AppMapper">

    <select id="findList" resultType="com.shikou.aicode.model.entity.App">
        SELECT
        id,
        appName,
        cover,
        initPrompt,
        codeGenType,
        deployKey,
        deployedTime,
        priority,
        userId,
        editTime,
        createTime,
        updateTime,
        isDelete
        FROM app
        WHERE id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
        <if test="!withDelete">
            AND isDelete = 0
        </if>
    </select>
</mapper>
